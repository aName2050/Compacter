const {
    Client,
    EmbedBuilder,
} = require('discord.js');

module.exports = {
    id: '',
    requiredBotPermissions: [],
    /**
     * @param {} interaction
     * @param {Client} client
     */
    async execute(interaction, client) {

    },
};
