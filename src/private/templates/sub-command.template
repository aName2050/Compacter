const {
  SlashCommandBuilder,
  ChatInputCommandInteraction,
  EmbedBuilder
} = require("discord.js");

module.exports = {
  subCommand: 'cmd.sub',
  /**
   *
   * @param {ChatInputCommandInteraction} interaction
   */
  execute(interaction, client) {

  },
};
