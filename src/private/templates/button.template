const {
    ButtonInteraction,
    Client,
    EmbedBuilder,
    PermissionFlagsBits
} = require('discord.js');

module.exports = {
    id: '',
    requiredBotPermissions: [],
    /**
     *
     * @param {ButtonInteraction} interaction
     * @param {Client} client
     */
    async execute(interaction, client, data) {

    },
};
